const canvas=document.getElementById("space"),ctx=canvas.getContext("2d"),progressBar=document.getElementById("progressBar"),heroCopy=document.querySelector(".hero-copy"),moon=document.querySelector(".moon-orb"),secret=document.getElementById("secret"),secretButton=document.getElementById("secretButton"),closeSecret=document.getElementById("closeSecret");let w,h,stars=[],meteors=[],mouseX=0,mouseY=0,lastMeteor=0;function resize(){w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+"px";canvas.style.height=innerHeight+"px";ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);stars=Array.from({length:Math.min(260,Math.floor(innerWidth/4))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.4+.2,a:Math.random()*.75+.15,phase:Math.random()*Math.PI*2}))}addEventListener("resize",resize);resize();addEventListener("mousemove",e=>{mouseX=e.clientX/innerWidth-.5;mouseY=e.clientY/innerHeight-.5});function draw(t){ctx.clearRect(0,0,innerWidth,innerHeight);stars.forEach(s=>{const x=s.x+mouseX*18,y=s.y+mouseY*12,a=s.a*(.72+.28*Math.sin(t*.0015+s.phase));ctx.globalAlpha=a;ctx.beginPath();ctx.arc(x,y,s.r,0,Math.PI*2);ctx.fillStyle="#fff";ctx.fill()});meteors.forEach(m=>{m.x+=m.vx;m.y+=m.vy;m.life-=.012;ctx.globalAlpha=Math.max(0,m.life);ctx.strokeStyle="#fff";ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(m.x,m.y);ctx.lineTo(m.x-m.vx*10,m.y-m.vy*10);ctx.stroke()});meteors=meteors.filter(m=>m.life>0);ctx.globalAlpha=1;if(t-lastMeteor>Math.random()*5000+2500){meteors.push({x:Math.random()*innerWidth*.8+innerWidth*.1,y:Math.random()*innerHeight*.35,vx:5+Math.random()*5,vy:3+Math.random()*3,life:1});lastMeteor=t}requestAnimationFrame(draw)}requestAnimationFrame(draw);addEventListener("scroll",()=>{const max=document.documentElement.scrollHeight-innerHeight,p=Math.min(1,scrollY/max);progressBar.style.width=p*100+"%";heroCopy.style.transform=`translate(${mouseX*10}px,${mouseY*8}px)`;moon.style.transform=`translateX(${-Math.min(520,scrollY*.32)}px) rotate(${scrollY*.02}deg)`});const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.18});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));secretButton.addEventListener("click",()=>secret.classList.add("open"));closeSecret.addEventListener("click",()=>secret.classList.remove("open"));